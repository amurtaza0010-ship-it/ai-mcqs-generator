import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "AI MCQ Generator - Create Quizzes from Documents",
  description:
    "AI-powered Multiple Choice Question Generator. Upload PDF, DOCX, or TXT files and generate high-quality quizzes instantly.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
